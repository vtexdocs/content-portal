// import { visit } from 'unist-util-visit';

// function rehypeSetTableWidth() {
//   return (tree: any) => {
//     visit(tree, 'element', (node) => {
//       if (node.tagName === 'table') {
//         node.properties = node.properties || {};
//         node.properties.style = 'width: 100%;'; // Define a largura desejada aqui
//       }
//     });
//   };
// }

// export default rehypeSetTableWidth;